# Environment Variables for The Other Dev Blog

# Eleventy automatically sets these during build:
# ELEVENTY_RUN_MODE=build (production) or serve (development)

# Optional: Node.js environment 
# NODE_ENV=production

# Optional: Disable colors in build output
# NODE_DISABLE_COLORS=1

# Optional: Enable Eleventy debug mode
# DEBUG=Eleventy*